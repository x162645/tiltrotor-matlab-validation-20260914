# 动态可信度研发入口（当前D03）

2026-09-11。长期目标：面向操稳的高效低阶动态建模，以独立数据上的精度—成本优势形成可比较成果；领先仍是待证明目标。

分支research/dynamic-fidelity-benchmark-20260910，Draft PR78。继承旧4cd7d4c/S00–S18和V1–V7，不覆盖PR77，不自动合并。先查真实HEAD与工作区，读AGENTS、CODEX_TASK和同一STEP_LEDGER。最新报告docs/research/dynamic_fidelity/D03_REPORT.md。

## 已有能力与证据角色

D01：四条NASA已识别动态参照及比较器，91实现检查；不是飞机模型预测，匹配工况缺口保留。
D02/D02.1：通用production动态原型与准定常对照，主要操纵通道65项一致性及12API检查通过。全部Jacobian/微小交叉通道不认领可靠。
D03：两个来源明确的平均轴向入流子模型接入同一15状态框架，保留旧假设松弛与13状态准定常。实际源方程、定常极限、正负总距、两档幅值/差分、频率诊断已完成；78检查通过，无新外部飞行验证。

## D03具体推进

NASA TM88327 Eq2/Eq4/Eq5，PP系数128/(75pi)、CF系数.849，采用表观空气质量而非拟合tau。只近悬停轴向范围；动态锥度betaDot泵吸在全机省略，明确非完整三状态PP、动态挥舞或翼尾尾迹。

源代码model/mean_inflow_88327.m；公开选项pp_mean/cf_mean；原气动力、执行机构、几何和默认模式不变。复用原合格工作点和旧时历，不重配平。两次执行均有十条新轨迹，首轮跨运行导数四项失败保留；同进程身份复查门限不变而通过，主要结果CSV逐字节一致。

物理提交ac78a0c；最后计算aea7936；永久证据55a91bbf。run34585998506与34586850178原MAT/CSV/日志和source PDF按run保存于evidence_d03，40文件3,910,766字节，不含索引；旧证据不改写。复查78项全部通过，升沉内部NRMSE<=.326%不是飞行精度。源式导致恢复尺度变化不等于模型更准确。

## 研发进度位置

阶段2从假设原型推进到来源明确的平均入流经典基线；阶段1的强竞争对手/匹配外部工况仍未齐备；阶段3原创低阶干扰和阶数选择、阶段4独立迁移验证、阶段5领先证据仍未完成。

下一步是入流—动态挥舞/锥度的有依据耦合及相应外部观测资格，不是再修一轮D02字段、换经验tau或一味加状态。优先复用现有叶素/挥舞力矩与原TM88327来源；依据状态物理和证据决定扩展一阶非均匀入流或翼尾尾迹，不把参考TF复制成新模型。没有新的原始数据时，明确区分数学验证、参考相关性和物理验证。

## 复现与权限

run_d03_source_inflow运行两个源模式及定向回归；旧D02完整套件保留为手动，避免重复触发。离线archive_d03_evidence.py --verify-only核原字节，不运行模型。必要回归不能省略，但无依赖变化不重做旧审计。

保护历史和他人更新，不强推、不自动合并、不隐藏失败、不未授权增加付费资源。所有当前运行已结束；授权自主技术决策不等于回复结束后后台无限运行。新结果继续同一台账，来源/方法/执行/限制必须可追溯。
