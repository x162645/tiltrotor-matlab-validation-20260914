# V6：同一机翼自由流迎角与源零升力矩归一化

2026-09-10，本契约与代码在V6运行结果产生前提交。继续原目标，不拟合目标，不晋升默认。

本轮已实际完成V4独立试验回归：OARF/WADC27点，Betzina24点；新旧差异小，但悬停绝对低估仍大。下一项是已有机翼输入链不一致，不重新调查整个旋翼。

读取V5原始四点MAT，发现机翼自由流区实际系数迎角与尾部下洗所用机翼迎角相差0.978481/0.946289/0.677134/0.434129deg。此数来自源公式及已有V5状态后处理，不是对目标拟合的迎角偏置。

已查看原CR166536 Sep1988RevA PDF137/138/139/142/249/383，分别为A69/A70/A71/A74/A181/B33。源SHA256：a2013a314af5beb0c5e9bc5bbeb26f99be9a8aa63f2ebe07deaaf11d0d44d67d。
A70要求机翼自由流系数查询alpha_WFS=alpha_F-K_XRW*X_RW*sum(C_RF)/max(.15,muMean)^2*57.3，A181力方向也用alpha_WFS；A74尾部下洗同样用它。V5实际机翼未应用此修正，尾部已应用。V6建立共享函数，尾部保持原算术结果，机翼自由流区采用同一有效角。动压仍为源q_free，不按角度修正虚构速度增量。

A70/A71另规定M0=q_free*S_W*c_W*Cm，Cm在近零升力选取；A69浸没区只列升阻，不能把这个整翼Cm在每一局部动压区域重复作为本征力矩。V6整翼本征力矩按总面积/自由流动压计算一次，左右各半归档；区域力臂力矩仍保留。

V6只闭合上述自由流子关系。V5滑流覆盖Eq16、浸没区来流Eq17、源CL/CD族和浸没区升阻保留；未实现完整GTRS尾迹投影、非均匀流系数K_FW、短舱独立干扰项。不是完整GTRS机翼复现，也不宣称共享来源相关性等于独立试验验证。

不改V4旋翼、控制、机身、几何参数、求解器、初值表或0.005容差。新增模式显式为GTRS_FREEFIELD_HELI_V6。V5和默认保留；不把其他源冲突强行修成匹配目标。

先执行原尾部逐值一致性、共享角相等、源动压/总面积力矩、浸没区力不变、覆盖/来流不变、坐标平移和域检查。通过后仍按原40/60/80/100四点各一次，失败保留，不设事后通过门槛。成本约4个原规模配平，预估数分钟，无密集速度或参数扫描。
