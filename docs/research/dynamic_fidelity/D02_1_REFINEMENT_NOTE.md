# D02.1 首轮失败与限定复查

2026-09-11。首轮run34580750797，计算13ca086，artifact10191624893，ZIP SHA256839e3f223abb442af4aae6515bc6bad5583305e79d041a6491113c23d67a9c4b。原始10条时历全部完成，65项检查中仅更严格工作点残差项失败；失败结果保留，不能改门槛宣布全过。

实际工作点x=0、cyclic=0，只有collective由原悬停helper求解。其xdot_u=-9.192352848e-5m/s²、qdot=1.378852927e-5rad/s²；垂向残差近零。加严旋翼内迭代已将入流导数残差降到6.41e-8，但固定theta/cyclic的单变量悬停求解仍留下纵向力和俯仰矩。来源是本次MAT中workpoint.trim及原trim_symmetric的hover分支，非新的文献猜测。

本修订仅在原配平的九状态导数最大值>=1e-5时，使用相同fminsearch算法作局部theta/collective/cyclic三变量平衡细化。目标是原方程X/Z/My平衡，不是外部响应；原参数、时间常数、默认配平和旧结果不改。不减f0、不忽略残余分量、不放宽1e-5检查。保留原productionTrim和完整refinement起终状态/调用数/代价。需要重新执行相同10条轨迹，因为工作点有实际改变。

首轮主通道NRMSE均小于0.34%，幅值减半绝对误差降到约1/4；这些结果不代替工作点失败。单边导数诊断暴露悬停wDot对u/v的持续左右导数差，须保留为原公式的不可微限制，不通过平滑或取消诊断隐藏它。后续若主通道通过，只认领该测试范围内的一致性，不称全Jacobian已验证。

D01被原宽泛目录触发器意外重复执行run34580750821，91项通过，仅更新旧证据运行元数据；不算新增D02验证。已缩小其自动触发到D01自己的代码/测试，保留手动触发，避免D02后续修改重复执行。
