# D14首次实线引导点拒绝（源读取失败，不是科学拟合失败）

在任何D14模型拟合或评分前，原始图4.34实线引导点的local (624,823)使x=1373的像素支持距离超过20px，提取程序抛出：`ValueError: Flight guide has no nearby ink: x=1373`。依据原生图的坐标网格复核，实线峰附近local624的纵坐标改为797。像素支持20px拒绝阈值未改变，未使用任何模型预测挑选轨迹。

唯一算法差异为extract_fig434.py的GUIDE中 `(624,823)` -> `(624,797)`。原始失败目录已在本轮执行环境保留；本文件及下列可逆改动保留在Git，不冒称首次失败含有尚未产生的外部评分或完整飞行目标CSV。输入CSV在失败前已产生，目标CSV未完成。

```
- (592,803),(609,826),(624,823),
+ (592,803),(609,826),(624,797),
```

随后对完整原图叠加采样点复核，计分前冻结316个输入和316个输出原像素列。多墨迹列保留包括相邻虚线在内的宽读图包络，故其排序结论为有意保守，而不是实测置信区间。
