# 缺失证据与恢复许可

机器清单见同名JSON。本包D11—D13原阶段清单完整；D14原源码可用但原运行结果未恢复；D15仅有此前已读报告正文与图像。前面报告曾称“本地已完成”，不等于这些临时文件如今仍可取回。

检索已尝试：当前容器全路径、当前对话Files列表/检索、D15原显示文件ID读取、Library定向检索及近期列表。D15报告原ID当前不可见；同一已返回工具响应仍可读取，因而只恢复其正文，不宣称字节副本。Library检索得到的旧D11/D12/D13和无关文档没有充作D15替代。

恢复顺序：先查用户明确授权的项目本机目录/此前运行工件；找不到则保留缺失记录，使用已有原数据和可见方程新建隔离恢复实现/结果。不修改旧代码，原失败仍保留；不能用一张曲线图或几项RMSE拼出未见的原CSV、原参数网格、原耗时与原执行哈希。

可以针对缺件做一次必要恢复运行，但不是重跑所有历史科学套件。D14提取代码及原PDF已保存，恢复其CSV不应改校准点/墨迹选择、不叫新数字化研究；D15新写程序必须标 NEW_REIMPLEMENTATION，不得标 FOUND_ORIGINAL_SOURCE。所有重建/复算输出用新的运行ID、时间、环境和代码哈希。

D11更早的D06—D10总ZIP本包没有再次全量嵌入；D11自带复算依赖已按原阶段保留。D05/更早仓库工作仍以已读回的远端为准。
