# D15 原报告正文转存

> 转存来源：本对话此前 Files 工具响应 `/response/turn125` 中 `D15_REPORT.md` 的完整正文。本轮只转存文本；原文件字节、源码和原结果未找回。以下叙述保留原报告的历史时间语义，不代表本次打包进行了这些计算。

---

# D15 本轮执行报告（本地已完成，GitHub未入库）

## 状态与主线
接手读回HEAD为1cf2419e36a05d49e24b0eeb317d6d6e18e4a298，PR78 Draft/open。D14远端只有计算约定、四Python文件与失败记录；不继承不存在的D14原生执行或原始结果归档。新D15_PLAN.md普通create_file被平台安全检查拦截，未生成提交。本轮未把同一上传改道、编码或转换工作流重试。受阻的是GitHub交付，下面新增数值实际为本地Python执行并读回。

主线继续是外部证据约束下的低成本动态预测；本步确定一个可执行的用途：已知未来功率杆指令和当前加速度时，短时间预测是否有用。不是新旋翼机理模型，不按状态/检查数或经典公式声称创新、国内领先。

## 来源和恢复
当前容器没有上一轮D14数值结果文件，仅有图像。以GitHub实际读回的原extract_fig434.py（git blob bb237eb3294f17c9260f2c23ea912359413040bc）和D12归档内的TM89428原PDF（SHA256 252f7df0c0e05bd8dece0a923d337df2231c0148f897f0376f930f6102b90ba2）恢复316输入/316观测行，评分窗口内为312观测，前段192、后段120。旧引导点和墨迹包络未改变，恢复不是新的首次数字化、独立试飞或盲测。图4.34位于PDF120/印刷97；来源式4.8为PDF110/印刷87。本轮NASA网页读取失败，复用已校验本地原件并查看实线覆盖图，不虚构成功网页截图或原始遥测。

## 1. 实际修正的预测语义
旧D14 predict_power_record在evaluation_time[0]重置状态，并仅在输出网格重建输入；稀疏输出会漏掉内部输入折点，截取后段会丢失先前记忆。原Git blob ae358bd8d1461ebb4b2008ffd04d4dd1762793f5原样保存，不修改旧研究身份。

新函数predict_known_command以origin_time和initial_acceleration_g为固定起点，包含全部延迟后的指令折点，精确推进ydot=-p*y-kappa*udot，输出网格不再决定输入或初态。超出输入记录的未来拒绝，功率杆百分数点/向下g单位门禁，复杂数与非有限数拒绝。输入前史仅允许明确的记录开始前常值假设。初态来自当前观测，不用未来输出优化。

合成三角输入被旧稀疏输出接口完全漏掉，新预测末端约0.001515g；另一截取后段案例旧接口返回零，新接口保留约-0.041911/-0.037734g响应。这是软件反例，不是新增试验。在D14实际0.02s计分路径中，最大修订差约4.8594e-5g，后段NRMSE约20.0037%变为20.0058%，不声称这一API修正推翻了旧中央成绩。

## 2. 有限时域条件预测
固定来源kappa=.0098,p=.105,delay=.0074，无新增物理参数。前段[.5,18)经验候选仅因推进器依赖变化定向重算，结果kappa=0.01188408715828304、p=0.09367172240024173；没有后段目标参与。该候选是命令层拟合，不安装到D13旋翼。

原观测时刻18至24s为出发点，三个预测时长共用65个出发点；目标取原观测中首个达到请求时长且不超过29s的点。因此实际时长为0.5309—0.5553s、2.0014—2.0911s和5.0053—5.0687s。195个重叠预测不算195次独立试验。所有方法共享同一当前加速度，后段目标只评分。

下表均为原读取值上的加速度RMSE，单位g；保持基线指未来加速度保持当前值。

|请求时长(s)|来源模型：提供未来指令|保持加速度基线|来源模型：未来指令保持当前值|
|---|---:|---:|---:|
|0.5|0.00615827|0.01313328|0.01320087|
|2|0.00651186|0.03596048|0.03391168|
|5|0.00808339|0.05415133|0.03996691|

已知未来指令下，来源模型相对保持加速度的名义MSE减少约78.01%/96.72%/97.77%。不知道未来操纵时，误差明显增大；不能把读取未来记录作为已知计划的成绩包装成不需要未来指令的预报。后者实现仅使用出发时刻已收到的输入点，不用下一点插值当前未知指令，已作未来输入篡改检查。

来源模型与前段重辨识模型并未在各时长都稳健排序；所有不利结果保留。静态增量基线同样接收当前加速度和已知指令，比只有持久值更强。在中央输入下，5s的来源模型相对静态增量的读取盒MSE差上界也小于零，但这项比较未扩展为全输入幅值盒保证，不混淆不同结论的边界。

## 3. 不只比较中央曲线：包含当前值、目标值及全部输入幅值盒
首轮按预先本地计划计算固定输入下共享初态/目标读图区间的二次函数极值。随后在读回中央成绩后，明确追加解析补充：分段线性输入使每个预测端点等于A*y_initial+w^T*u。按所有原输入采样点的读图上下界独立变化，计算w^T*u的精确区间；再对来源模型和保持加速度的平方损失差，在初态、目标和强迫响应区间上求极值。每个预测的三变量盒极值通过边界和二次驻点计算，跨预测的和是共享记录的保守外包，不是概率区间或外向舍入机器证明。

输入全样点幅值盒、初态/目标读图盒一并纳入后，约2s和5s的来源模型平均平方误差仍低于保持加速度，在-0.03、0、+0.03s三个分别固定的输入整体时间偏移情景均成立；0.5s范围仍不能稳健排序。三种离散时间偏移不是连续任意时间误差的覆盖，源p/k参数统计不确定度、真实传感噪声及模型形式误差亦未由这些读图盒覆盖。

## 4. 执行、读回与成本
本地Python/NumPy/SciPy/pandas的实际版本、运行时刻和全部源代码/数据哈希在run_01/EXECUTION_MANIFEST.json。无新MATLAB、CI或外部实验。只做新推进器与变更的预测依赖，不跑D06—D13科学套件。

输出网格不变性差约1.39e-17g；独立增广状态矩阵指数与新标量推进器差不超过5.56e-17g；后段输出篡改不改变前段参数；未来输入篡改不改变未知指令保持分支。输入线性权重与推进器最大差4.86e-17g。各指标已从落盘ALL_FORECASTS.csv和区间明细重新计算，不只看程序退出码。

补充类型门禁仅拒绝复杂数/错误描述类型，不改实数方程。修改前完整脚本保留preserved_before_type_guard，原研究身份可按哈希回查；真实记录的门禁补丁前后差约1.25e-16g，只做定向重放，不重拟合。

同一Python环境，195个来源模型端点预测的五次中位耗时约18.48ms；包括调用与输入折点处理，不是硬实时保证、MATLAB性能或全机加速结论。

## 5. 当前用途决定与余项
来源一阶模型可作为该已有飞行记录条件下、已知指令和当前加速度的低成本经验预测基线。证据增量是用途明确且包含读取不确定性的对照，不是一个新的物理状态模型或已证实的国内领先方法。

尚未闭合同构型实际总距/执行器链、匹配质量/转速/环境、跨独立记录验证、D13旋翼载荷/全机预测、源参数置信区间及完整期刊原创性。不能用经验命令模型替代旋翼部件验模声明。

GitHub文件交付仍受平台安全检查阻断，远端不能称D15已完成。旧台账到D13、D14部分代码到1cf2419保持原状；本地同一台账追加包含D14部分状态和D15实际记录。用户不需要嵌套下载包，本轮未重新制作历史总包。没有回复后自动后台执行承诺。
