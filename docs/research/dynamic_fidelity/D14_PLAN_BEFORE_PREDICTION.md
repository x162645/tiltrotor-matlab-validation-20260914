# D14 预测计算前约定：公开实飞时历挑战与输入层级分流

2026-09-11。接续实际D13 HEAD c952555b0fabf221a67ddea6dea522daae87051c。用户要求继续执行并直接上传GitHub，不再交付反复嵌套下载包。旧D06-D13成果不重跑；原台账/失败/校准分支保留；PR78保持Draft不合并。

## 实质目标与来源

主线仍是面向操稳/控制、受外部证据约束的低成本动态预测。本步不再以查表对同方程的0.016%数值差代替实飞误差，直接利用已有NASA TM89428图4.34的功率杆输入和实飞向下加速度，进行因果时域预测检验并与成熟一阶方法对照。

源PDF SHA256 252f7df0c0e05bd8dece0a923d337df2231c0148f897f0376f930f6102b90ba2，D12已永久归档。图4.34为PDF120/印刷97，解释续PDF121/印刷98；式4.8在PDF110/印刷87。PDF21定义输入为功率杆增加的百分数点而非实际总距rad；PDF38说明调速器会改变功率杆到总距的关系；PDF44-45给出悬停一般试验条件，但未提供本时历完整质量/气象/转速记录。源50Hz测量预滤波及其时域验证输入滤波说明不能冒充本次具体滤波器完全已知。

本记录是新数字化的旧公开飞行图，不是新试飞、原始遥测或新盲测。图已可见；预声明的时段只表示计算上不让后段目标参与拟合，不声称统计独立。不得把虚线模型当实飞真值。原生2480x3274像素支持、仿射坐标和手工实线引导点将全部保留，歧义列保留可见墨迹并集读图包络。包络不是测量置信区间。像素支持初次拒绝一处引导点，依据原图纠正并保留失败；此时尚未拟合或评分。

## 固定预测与数据分工

时间轴保持原图秒数。共同起点0.5s，终点29s；输入在起点以前视为恒定，状态按该输入平衡初始化，不优化初态、时间对齐或输出偏置。输入按线性插值，模拟dt=.02s；在原实飞读取时刻计分。前段[0.5,18)可用于明确的经验辨识；后段[18,29]严格不进入拟合。边界依据原报告前缓后急的两类输入，在本轮任何模型评分前固定。

1. SOURCE_FROZEN_1STATE：式4.8，G=-.0098*s/(s+.105)*exp(-.0074*s)，系数完全固定；正确功率杆百分数点输入，向下g输出。其系数来自原频域识别而非当前图反求，是成熟、证据角色不同的对手。
2. PREFIX_IDENTIFIED_1STATE：同一经典一阶结构，只有经验增益kappa与衰减率p在前段辨识；p在[.02,1]s^-1。先161点固定对数网格再局部有界最小化，kappa为解析最小二乘正增益。固定来源描述延迟.0074s，不拟合新延迟或机械参数。它是命令层经验模型，不是旋翼物理参数。
3. STATIC_GAIN_NEGCONTROL：-.0098*(u-u0)，同一来源增益、无动态负对照，明确不是真实气动候选。
4. D13_SHAPE_PREFIX_GAIN_DIAGNOSTIC：固定D13局部两状态形状/工作点，仅给予与经验模型相同前段数据中的一个正增益自由度，用于对未知常数输入映射作条件诊断。该增益不得安装到旋翼或解释为测得的总距换算，不能授予部件级实机验证资格；所有诊断结果保留，不根据名次修改b/k/质量/工作点。只用PP代表，不重复CF/PP小名次扫描。

既有D13非线性接口仍要求真实报告总距rad；不得把本图功率杆百分数偷偷作为其输入。新命令层经验预测器必须拒绝总距rad/0-1比例单位、桨毂力/总推力用途和未声明来源域。SOURCE和PREFIX的经验身份与D13物理条件身份分开。

## 指标、敏感性与不利结果

主结果保留原读取实飞波动，报告RMSE(g)、NRMSE(相对实飞RMS)、最大绝对残差；前段、后段、全段分别保存。无临时5%或10%飞机通过门槛。补充只对两侧输出施加同一因果二阶Butterworth低通3rad/s作为观察尺度，不作为新增物理状态、拟合通道或原报告处理的精确复制。原时历频谱含超来源频带内容，尤其急输入段；保留而不删点，不能从全时域计分推断严格频带保证。

固定参数后的输入读图下/上界、输入时间移位+/-0.03s、PCHIP插值为预设敏感性情景，不选最有利情景。输出共同读图区间下的成对平方误差差范围单列，零覆盖只代表不能据此排序；不是置信检验。后段目标篡改检查不得改变前段参数。dt减半只查新时域数值依赖，不运行历史套件。

计算前明确允许成熟一阶方法优于物理候选。即使某方法拟合较好，也不以这一个已见图证明跨工况/外推/国内领先或论文原创性。先前无校准来源描述、经验前段辨识、D13映射不足分支均保留。

## 实施与真实执行

源码、实线像素坐标、源定义、原始输出、参数冻结文件和错误记录均直接入Git。原生MATLAB对新预测/辨识和Python结果作定向核验，原始MAT/CSV/JSON实际读回；不把检查数当外部样本数，不追溯改写D06-D11执行身份。只运行D14新任务，保护他人更新，非强制快进，最后接续同一STEP_LEDGER。
