# D04有限延伸：保留共同动态而非直接删除锥度

主运行08a69fa已完成132检查及16轨迹，删掉动态锥度后中高频差异显著。本补充在读取该结果后提出，不伪称未看结果的盲研究；它不拟合任何参数。

对相同旋翼、相同初始状态、相同实际总距，双旋翼方程有交换对称性。差动子空间不被共同输入激励。将7状态中的vi/beta/betaRate两侧各压成一个共同状态，保留w共4状态；按总质量除以旋翼数、单旋翼几何/载荷不变构造等价机械方程。输入输出映射和工作点均由方程得来，不作平衡截断或近似拟合。

这只能应用于本模型的对称不变子空间，不支持差动初始条件、差动操纵、翼尾不对称和旋翼互扰，更不是新增第二种飞机构型。数学对称降阶是经典方法，不认领原创或国内领先。

测试固定状态下的非线性等价、投影后A/B与实际小模型、频响、共同输入时间响应，并拒绝非共同输入。PP/CF各一组全/降模型，4条新增3s时历，原主16轨迹不重复。时间比较同进程同求解器精度，但短时长/JIT噪声保留，不预定加速倍率。

实现门限：固定状态导数/输出相对差1e-12；数值Jacobian/频响相对差1e-8；时域最大按各通道max(1,maxabs(y))尺度归一差1e-7。所有为数学/数值实现条件，不是实飞误差标准。工作完成后统一D04台账和报告，不产生新的总主线。
