# D16 保留的实现与读回修订

首次代表和72案例原始数据在native_representative_v1/native_full_v1；源码2f8bdcf，完整运行结束时HEAD2fa64fd（D16源码相同）。这些运行确实执行，无科学异常退出。

在原始读取复核中发现：`unique([grid;on;off])` 将0.3与0.30000000000000004等保留为两个输出，造成某些折点几乎重复采样，违反统一0.002s网格意图。修正为已有近邻网格点对齐准确折点、只保留一次。保留v1原结果，v2重算72案例是软件修正的必要回归，不是改输入时窗/阈值或新的外部试验。

同时将直接源的输出按128时刻批量计算，每个恒定输入段缓存B(theta,lambda_s)，并将各廉价模型输出向量化；径向求积和C81查值未改变。最终成本使用优化后的同环境完整预测，旧耗时不作为主要速度结论。

首轮Python读回readback_v1在反序列化后的cell中遇mat_struct属性读取差异，TypeError发生于第一个MAT/CSV对照，未给出通过结果。修正字典/对象访问，不修改MAT或原CSV；旧脚本源码会随版本历史保留，失败log归档。新读回使用readback_v2。
