# D03首轮导数身份检查与同进程复查

2026-09-11，首轮run34585998506 / artifact10193658400 / ac78a0c。全部十条数值时历及78项检查已执行；74项通过，4项失败均为新算导数与既有跨运行归档导数的非入流行一致性。

实际读回MAT确认：A非入流行最大差7.5791e-12；C最大差1.46466e-8，后者在Fx对总距的导数约-3.561399N/rad处，相应2h=2e-5rad，等效力差约2.93e-13N。B和D相同；工作点原始RHS/观测逐值一致，新PP/CF非入流矩阵互相一致。首版两项断言的value误写0，真假判定仍正确；原记录不改写。

这不能直接证明跨运行差异的底层原因。因此复查只在同一MATLAB进程中重新求原dynamic基线两档Jacobian，再以原1e-12门限比较非入流行和观测矩阵；保留跨运行差异及归档旧矩阵作单独诊断。没有修改物理、数值步长、求解容差、输入、来源质量系数或验收门限。

既有工作点和旧正向时历仍复用，不重配平；只增加两次小规模线性化。该复查回答同一次计算环境下改入流方程是否改变其他方程，不把跨运行差异悄悄放宽成通过。若同进程仍不一致，继续定位，不覆盖首轮失败。

首版newPhysicalCases=10含义为十条飞机数值时历，不是十个真实试验；后续manifest明确改为newAircraftTrajectories=10、newExperimentalSamples=0。所有解析解检查都是软件/数学核验，非实验验证。
