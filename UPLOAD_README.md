# tiltrotor-general-validation

The GitHub repository contains the reproducible project package as `tiltrotor-general-validation-package.zip`. The archive includes MATLAB model, analysis, tests, documentation, and compact executed external evidence. The local Git transport was unavailable during this upload, so the package archive is used for complete delivery.

Latest local commit: b01436e.
